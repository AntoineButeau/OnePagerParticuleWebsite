/**
 * Particleground demo
 * @author Jonathan Nicol - @mrjnicol
 */

$(document).ready(function () {
    $('body').particleground({
        dotColor: '#000',
        lineColor: '#000',
        lineWidth: '0.1',
        particleRadius: '3',
        parallaxMultiplier: '8'
    });
});